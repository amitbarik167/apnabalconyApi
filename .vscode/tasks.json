{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "docker-build",
			"label": "docker-build",
			"platform": "node",
			"dockerBuild": {
				"dockerfile": "${workspaceFolder}/apnabalconyapi/Dockerfile",
				"context": "${workspaceFolder}/apnabalconyapi",
				"pull": true,
				"tag": "apnabalconyapicontainer.azurecr.io/apnabalconyapi:latest"
			},
			"node": {
				"package": "${workspaceFolder}/apnabalconyapi/package.json"
			}
		},
		{
			"type": "docker-run",
			"label": "docker-run: release",
			"dependsOn": [
				"docker-build"
			],
			"platform": "node",
			"node": {
				"package": "${workspaceFolder}/apnabalconyapi/package.json"
			}
		},
		{
			"type": "docker-run",
			"label": "docker-run: debug",
			"dependsOn": [
				"docker-build"
			],
			"dockerRun": {
				"env": {
					"DEBUG": "*",
					"NODE_ENV": "development"
				}
			},
			"node": {
				"package": "${workspaceFolder}/apnabalconyapi/package.json",
				"enableDebugging": true
			}
		}
	]
}